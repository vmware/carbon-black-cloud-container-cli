This image is build manually with the main goal to test an image with interesting files and how they interact across the layers.
The app under ./source/_app is meant to be both an executable (for tests) and a helper to change/delete files within a SCRATCH image that lacks any tooling.
